# Supplementary data tables

`build_supplement_tables.py` is the script that assembled the supplementary data tables deposited with the preprint from the
internal collection directories; it documents their provenance and is not runnable from the repository. The tables themselves
are the inputs of every analysis in `expression_splicing/` (see `../README.md` for the list and where to put them).
